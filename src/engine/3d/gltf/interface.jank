(ns engine.3d.gltf.interface
  (:refer-clojure :exclude [load])
  (:require [engine.3d.gltf.core :as core]))

(defn parse [{:keys [_path] :as args}]
  (core/parse args))

(defn load [{:keys [_model _base-path] :as args}]
  (core/load args))
